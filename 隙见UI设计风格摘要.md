# 隙见 XIJIAN UI 设计风格摘要

> 基于两张品牌海报确定的产品设计语言。海报负责建立记忆，工作台负责承载高频研究操作；两者共享色彩、字体气质和信息隐喻，但不直接复制版式。

## 一、设计风格

**风格名称：编辑部证据工作台（Editorial Evidence Workbench）**

核心气质：

- 编辑感：强标题、明确数字、中文优先，像一份正在被整理的研究稿。
- 证据感：评论、数量、评分和原始出处始终比装饰更重要。
- 纸张感：暖米白提供温度，白色工作面保证长时间阅读清晰。
- 断裂感：橙色代表从评论中被发现的机会，只出现在关键节点。
- 克制感：产品界面保持紧凑、稳定，避免把每个区块都做成宣传卡片。

## 二、海报视觉结论

### 1. 色彩关系

海报的识别度来自四层对比，而不是单一暖色：

1. 近黑色承担标题、结论和品牌权威感。
2. 纸张米白构成环境与留白。
3. 裂隙橙标记机会、关键数字和推进动作。
4. 低饱和数据蓝标记评分、证据数量和可信信息。

评论灰只承担背景信息，不参与主要操作层级。

### 2. 排版关系

- 标题使用高字重中文黑体，短句、紧凑换行、强结论表达。
- 数字可独立放大，但必须附带清晰单位或标签。
- 英文仅作为品牌辅助信息，不使用夸张字距。
- 正文保持常规字重和充足行高，避免海报标题字重扩散到所有内容。

### 3. 图形关系

- “裂隙”是发现机会的隐喻，可转译为橙色边线、进度状态和当前导航。
- 评论气泡用于原始评论证据，不作为通用容器造型。
- 箭头和编号用于表达分析流程，不作为无意义装饰。

## 三、产品色彩系统

| Token | 色值 | 产品用途 |
|---|---|---|
| `--canvas` | `#F3EFE6` | 页面环境、纸张米白 |
| `--surface` | `#FFFDF8` | 主工作面、卡片、输入区域 |
| `--surface-muted` | `#F8F4EC` | 次级区域、评论背景 |
| `--ink` | `#11110F` | 标题、结论、关键正文 |
| `--muted` | `#625F58` | 正文说明 |
| `--subtle` | `#969087` | 辅助信息、来源、空状态 |
| `--line` | `#E3DBCF` | 普通边框和分隔线 |
| `--line-strong` | `#CFC4B6` | 强分隔、引用边线 |
| `--accent` | `#F04A17` | 裂隙、机会、主按钮、关键数字 |
| `--accent-hover` | `#D83B0D` | 主操作悬停状态 |
| `--accent-soft` | `#FFF0E8` | 橙色低强度状态背景 |
| `--blue` | `#4F86CA` | 评分进度、证据数量、数据提示 |
| `--green` | `#39715C` | 完成、有效、成功状态 |
| `--amber` | `#A76522` | 风险和待核验状态 |

### 配色比例

- 纸张米白与白色工作面约占 80%。
- 墨黑与灰色文字约占 15%。
- 橙色、蓝色及状态色合计不超过 5%。

橙色不能铺满大面积容器；蓝色不与橙色争夺主操作，只服务于数据和证据。

## 四、字体与层级

| 层级 | 建议尺寸 | 字重 | 用途 |
|---|---:|---:|---|
| 品牌海报标题 | `48px - 72px` | `900` | 宣传物料、首页核心句 |
| 页面标题 | `25px - 32px` | `800 - 900` | 调研、分析、机会、Brief 页头 |
| 内容标题 | `17px - 20px` | `700 - 800` | 机会标题、Brief 标题 |
| 核心分数 | `28px - 36px` | `800 - 900` | 隙值、重点指标 |
| 正文 | `13px - 16px` | `400 - 500` | 说明、评论、洞察 |
| 辅助文字 | `11px - 12px` | `500 - 700` | 标签、来源、步骤 |

产品界面不按视口宽度连续缩放字体；仅在明确断点切换排版。

## 五、组件规范

### 页面与容器

- 页面以完整工作区为单位，不把每个段落包成悬浮卡片。
- 单个结果、证据、Brief 可以使用独立容器。
- 圆角统一为 `6px - 8px`，阴影只用于区分层级。
- 页面结构统一为“页头说明、操作工具条、结果内容”。

### 按钮与导航

- 主按钮使用裂隙橙填充和白色文字。
- 次按钮使用纸白背景、深色文字和暖灰边框。
- 当前导航使用橙色底边，不使用大面积彩色标签。
- 流程步骤使用 `01、02、03` 等明确编号。

### 数据与评分

- 隙值使用橙色，强调“机会判断”。
- 评分条与证据数量使用数据蓝，强调“依据”。
- 成功状态使用绿色，风险提示使用琥珀色。
- 所有数字必须同时显示名称、单位或解释。

### 评论证据

- 评论采用浅纸色背景、细边框或左侧引用线。
- 评论正文保持高对比，不使用大面积低透明度。
- 昵称、头像、主页信息不进入视觉设计。

## 六、品牌元素使用边界

### 可以进入产品界面

- 橙色顶边、当前状态线和进度线。
- 黑色重标题与橙色关键数字。
- 数据蓝评分条。
- 评论引用样式和流程编号。

### 仅用于海报或品牌传播

- 贯穿整屏的真实撕裂图形。
- 大量漂浮、模糊或倾斜的评论气泡。
- 胶带、回形针、纸张叠放等拟物装饰。
- 超大标题覆盖业务操作区。

这些元素在宣传物料中负责制造记忆，在工作台中会干扰筛选、对比和连续阅读。

## 七、响应式原则

- 桌面端优先支持扫描、并排比较和连续操作。
- 移动端导航横向滚动，筛选器转为纵向排列。
- 指标条由五列降为三列，机会分数移动到卡片顶部。
- 不允许标题、筛选标签、按钮文字和数据指标互相遮挡。

## 八、验收清单

- [ ] 页面是否保持纸张米白环境和白色工作面？
- [ ] 近黑色是否承担主要信息，而不是大面积橙色？
- [ ] 橙色是否只用于机会、主操作和当前状态？
- [ ] 数据蓝是否只用于评分与证据？
- [ ] 评论气泡是否仅用于评论证据？
- [ ] 卡片圆角是否不超过 8px？
- [ ] 页面是否仍然适合高频筛选和连续阅读？
- [ ] 桌面端与移动端是否无内容遮挡和横向溢出？
