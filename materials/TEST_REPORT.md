# 隙见比赛版本测试记录

测试日期：2026-07-30

## 自动化结果

- `122 passed`，8 条警告均来自 Gradio/FastAPI 弃用提示。
- CSV、XLSX、粘贴文本、示例、空文件、编码异常、重复和无效评论均有测试覆盖。
- AI 非法 JSON 自动重试；超时受重试次数和超时配置约束。
- 向量化或聚类异常时回退到关键词主题归并。
- Brief 差异性、证据绑定、安全表达、单份重生成、SQLite 和 Markdown 导出均有测试覆盖。
- 11 类产品事件、敏感元数据拦截、漏斗计算、500 条清洗时限及公式/脚本文本安全均有测试覆盖。

## 产品埋点

- 事件库记录事件名、任务 ID、机会 ID 和安全元数据，不保存评论正文。
- 已通过真实页面流程验证示例、导入、任务、分析、机会、证据、Brief、复制和 Markdown 导出事件。
- 机器可读漏斗结果见 `analytics_summary.json`。

## 连续流程

`scripts/validate_release.py` 连续运行 5 次示例主链路：

| 指标 | 结果 |
|---|---:|
| 成功运行 | 5 / 5 |
| 核心流程完成率 | 100% |
| 机会证据可追溯率 | 100% |
| Brief 证据可追溯率 | 100% |
| 数字程序计算率 | 100% |
| 正式机会 | 6 |
| 低频弱信号 | 4 |

机器可读记录见 `release_metrics.json`。

## 浏览器验收

- 已实测首页到 Brief、证据筛选、单方案重生成、Markdown 导出和刷新恢复。
- `1366×768`、`1920×1080` 和 `390×844` 检查页面横向溢出。
- Chrome 内核真实浏览器完成主流程。Edge 需在部署公开链接后用目标设备补做兼容性确认。

## 统计复核

- 评论数来自唯一有效评论 ID 集合。
- 覆盖率为机会唯一评论数除以有效评论数。
- 隙值由覆盖度、需求张力、情绪强度、人群清晰度和内容转化度加权计算。
- 发布验收脚本对每个正式机会重新计算以上数字并逐项比较。
